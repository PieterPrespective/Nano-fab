/**
 * Layer 4 — fab yield models (milestone 4, NOT part of phase 1).
 *
 * Planned: Poisson yield Y = exp(-A*D0), Murphy yield, dies-per-wafer,
 * cost per good die. Pure functions, test-first.
 */

export {};
