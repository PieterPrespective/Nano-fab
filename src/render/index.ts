/**
 * Rendering: Canvas 2D plot/UI renderers (phase 1), WebGL2 aerial-image
 * renderer (milestone 3).
 *
 * Phase 1: plot.ts — Id–Vg curve plotting (log/linear scales, axis layout as
 * pure functions so they are unit-testable; canvas calls kept thin).
 */

export {};
