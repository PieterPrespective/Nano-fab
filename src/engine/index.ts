/**
 * Game engine: loop, state machine, level loading/validation, scoring,
 * save/load (localStorage).
 *
 * Phase 1 modules (test-first, see prompts/nf01/03-engine-levels-scoring.md):
 *   - levels.ts   — level JSON schema parsing + validation
 *   - scoring.ts  — target checks + star scoring from device metrics
 *   - progress.ts — save/load progress behind a storage interface
 */

export {};
