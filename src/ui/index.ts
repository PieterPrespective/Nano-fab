/**
 * UI: control panels (sliders/choices), level select, results panel, and the
 * "explain the physics" codex panel. Touch-first (drag, pinch-zoom).
 *
 * Keep UI thin: all game logic lives in /engine, all math in /physics.
 */

export {};
